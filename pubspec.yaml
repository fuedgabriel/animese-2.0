name: animese
description: Aplicativo de anime
version: 2.1.5+1

environment:
  sdk: ">=2.6.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^0.5.7  #dados no celular
  http: ^0.12.1 #Rquisição
  video_box: ^0.12.0 #Player de vídeo
  cached_network_image: ^2.2.0 # Loading image
  firebase_admob: ^0.9.3+2 # Propaganda
  wakelock: ^0.1.4+1 #Lock wake
  download_manager: ^1.0.1+2
  auto_size_text: ^2.1.0









dev_dependencies:
  flutter_test:
    sdk: flutter


flutter:
  assets:
    - assets/logo/NameRe.png
    - assets/logo/Name.png
    - assets/logo/NameIcon.png
    - assets/logo/Icon.png
    - assets/imgs/loading.gif


  uses-material-design: true

